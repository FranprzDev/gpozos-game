"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
type Result = { id: number; player_name: string; time_ms: number; coverage: number; deficit: number; matches: number; total_cost: number };
const fmt = (ms: number) => `${String(Math.floor(ms / 60000)).padStart(2, "0")}:${String(Math.floor(ms / 1000) % 60).padStart(2, "0")}`;
export default function Ranking() { const [rows, setRows] = useState<Result[]>([]); useEffect(() => { fetch("/api/results").then(r => r.json()).then(setRows); }, []); return <main className="ranking-page"><header><div className="brand">⌁ GPozos <b>·</b> SAT</div><nav><Link href="/">Volver al desafío ←</Link></nav></header><section className="ranking-hero"><span className="eyebrow">MODELO DE PROGRAMACIÓN LINEAL</span><h1>Ranking de gerentes</h1><p>Ordenado por coincidencias con el plan PL, cobertura, menor déficit y tiempo.</p></section><section className="ranking-table"><table><thead><tr><th>#</th><th>Gerente</th><th>Coincidencias</th><th>Cobertura</th><th>Déficit</th><th>Tiempo</th></tr></thead><tbody>{rows.map((r, i) => <tr key={r.id}><td>{i + 1}</td><td>{r.player_name}</td><td>{r.matches}/3</td><td>{r.coverage.toLocaleString("es-AR")} hab.</td><td>{r.deficit.toLocaleString("es-AR")}</td><td>{fmt(r.time_ms)}</td></tr>)}</tbody></table>{!rows.length && <p className="empty">Todavía no hay partidas completadas.</p>}</section></main>; }
